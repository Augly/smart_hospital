<!--index.wxml-->
<view class="container">

  <view class="message" wx:for="{{messageList}}" wx:key='' wx:for-index="index" wx:for-item="item">
    <left-smooth bind:del='del' myindex="{{index}}">
      <!-- <view class="message-item" catchtap='res'>
        <view class="message-title">{{item.user_message_title}}</view>
        <view class="message-brief">{{item.user_message_content}}</view>
        <view class="message-right">
          <view class="message-reminder" wx:if="{{item.message_state==2}}"></view>
          <view class="message-date">{{item.user_message_createtime}}</view>
        </view>
      </view> -->
      <view class='item' catchtap='res' data-id='{{item.user_message_id}}'>
        <view class='item_head'>
          <text class='time'>{{item.user_message_createtime}}</text>
          <text class='send'>发件人：总控后台</text>
          <text class='type'>未读</text>
        </view>
        <view class='item_content'>{{item.user_message_content}}</view>
      </view>
    </left-smooth>
  </view>
  <view class='nodata' wx:if="{{messageList.length==0}}">暂无消息</view>
</view>
<image class='tochat'  src='/images/120896888757042458.png'></image>